# Copy to `.env.local` for local sign-in. On Render, set the same keys in the dashboard.
# The app runs without these — weeks stay on the device until they are set.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
